# Brand-Vision
React + Vite dashboard for real-time vehicle brand detection — live stream viewer, detection charts, and history, built with TypeScript and shadcn/ui. FastAPI service that detects vehicle brands from live traffic camera streams using YOLO + a custom brand classifier, with WebSocket live events and SQLite stats.
